<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.chengfu.fuexoplayer2.demo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name=".APP"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.chengfu.fuexoplayer2.demo.MediaChooseActivity"/>
        <activity
            android:name=".VideoPlayerActivity"
            android:screenOrientation="portrait"
            android:theme="@style/ActivityPlayerTheme" />

        <activity android:name=".ui.video.VideoListActivity"/>

        <activity android:name=".ui.audio.AudioListActivity"/>

        <activity
            android:name=".AudioPlayerActivity"
            android:screenOrientation="portrait"
            android:theme="@style/ActivityPlayerTheme" >

        </activity>

        <service android:name="com.chengfu.fuexoplayer2.MusicService">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>
        <provider
            android:authorities="com.chengfu.fuexoplayer2.audio.MusicProvider"
            android:exported="false"
            android:name="com.chengfu.fuexoplayer2.audio.MusicProvider"/>
    </application>

</manifest>